export enum RootNote {
  A = "A",
  B = "B",
  C = "C",
  D = "D",
  E = "E",
  F = "F",
  G = "G",
}

export enum KeyType {
  MAJOR = "major",
  MINOR = "minor",
}

export enum ScaleType {
  MAJOR_PENTATONIC = "major pentatonic",
  MINOR_PENTATONIC = "minor pentatonic",
  MAJOR = "major",
  MINOR = "minor",
}

export enum AccidentalType {
  SHARP = "sharp",
  NATURAL = "natural",
}

export enum SortPosition {
  LOCATION = "location",
  NUMBER = "number",
}

export enum Orientation {
  PORTRAIT = "portrait",
  LANDSCAPE = "landscape",
}
